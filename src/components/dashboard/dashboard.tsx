import { Container, Header, Subtitle, Title } from "./style"

export default function Dashboard() {
    return (
        <Container>
            <Header>
                <div>
                    <Title>Dashboard</Title>
                    <Subtitle> Dashboard App </Subtitle>
                </div>
            </Header>
        </Container>
    )
}